{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the main Mistake Trainer window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
