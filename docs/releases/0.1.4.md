# 0.1.4 release evidence

This ledger records evidence for the `0.1.4` candidate. `PENDING` rows are release blockers and
must not be inferred from local unit tests.

## Candidate identity

| Item | Status | Evidence |
| --- | --- | --- |
| Version identity | PASS | `package.json`, `src-tauri/Cargo.toml`, `src-tauri/Cargo.lock`, and `src-tauri/tauri.conf.json` declare `0.1.4`. |
| Immutable candidate commit | RECORDED | Candidate branch commit will be recorded after the version bump is committed and pushed. |
| Successful PR CI for the candidate | PENDING | Record the exact GitHub Actions run URL and matching head SHA. |
| Merge commit on `main` | PENDING | Merge only after all required PR checks pass. |
| Successful push CI for the merge commit | PENDING | Record the exact push CI run and matching merge SHA. |
| Annotated tag `v0.1.4` | PENDING | Create only from the reviewed `origin/main` merge commit after same-SHA push CI succeeds. |

## Deterministic gates already observed locally

| Gate | Status | Evidence |
| --- | --- | --- |
| Production dependency audit | PASS | `pnpm security:audit` passed with no known vulnerabilities. |
| Frontend regression suite | PASS | 177 test files and 1,027 tests passed in the final remediation run. |
| Frontend lint, typecheck, and production build | PASS | `pnpm lint`, `pnpm typecheck`, and `pnpm build` passed; the existing large PDF chunk warning is non-fatal. |
| Rust all-target tests and strict lint | PASS | The bounded Windows runner, `cargo test --all-targets`, Clippy with `-D warnings`, and format checks passed locally. |
| Generated bindings stability | PASS | Regeneration was repeated and preserved SHA-256 `28968581EF1E871FDDD5D4B110A70F94A5CB763E82E7049A0A6E9690F4DB52A1`. |
| Architecture and boundary contracts | PASS | Frontend dependency-direction and Rust boundary contracts passed locally. |

## Release artifacts and physical validation

| Gate | Status | Evidence |
| --- | --- | --- |
| x64 installer build and GUI smoke test | PENDING | Must pass on the immutable merge/tag candidate in GitHub Actions. |
| ARM64 installer build and GUI smoke test | PENDING | Must pass on the immutable merge/tag candidate in GitHub Actions. |
| Signatures, checksums, and updater manifest | PENDING | Verify all expected Release assets and manifest architecture entries. |
| Supported Windows/DPI/device validation | PENDING | Capture physical validation results before publishing beyond draft status. |
