# 0.1.3 release evidence

This ledger separates evidence observed on 2026-08-19 from work that still requires an immutable candidate, CI, installers, or physical Windows devices. `PENDING` rows are release blockers and must not be inferred from unit tests.

## Candidate identity

| Item | Status | Evidence |
| --- | --- | --- |
| Version identity | PASS | `package.json`, `src-tauri/Cargo.toml`, `src-tauri/Cargo.lock`, and `src-tauri/tauri.conf.json` declare `0.1.3`. |
| Audited baseline commit | RECORDED | Local baseline before the current uncommitted remediation is `9e04bd65a2e6ab7113ca4550160e347a048c15d2`. |
| Immutable candidate commit | PENDING | Record the reviewed commit after the remediation and unrelated smart-question-organizing work are separated or intentionally included. |
| Successful push CI for the candidate | PENDING | Record the exact GitHub Actions run URL and matching head SHA. |
| Annotated tag `v0.1.3` | PENDING | Create only from the reviewed `origin/main` candidate after same-SHA push CI succeeds. |

## Observed deterministic gates

| Gate | Status | Evidence |
| --- | --- | --- |
| Production dependency audit | PASS | `corepack pnpm security:audit` exited `0` with no known vulnerabilities after pinning `nanoid 3.3.18`. |
| Frontend regression suite | PASS | The final post-remediation coverage run passed 177 files and 1,027 tests, including the 23-test `LibraryView` suite. |
| Frontend lint and production build | PASS | `corepack pnpm lint` and `corepack pnpm build` exited `0` after the local-day and paginated-library changes. The existing large PDF chunk warning remains non-fatal. |
| Report local-calendar regression | PASS | Negative-offset midnight and positive-offset 16-day streak tests passed; report and dashboard now share the full-history streak result, and all 15 `ReportView` tests passed. |
| Bounded problem listing | PASS | 45 duplicate-timestamp fixtures crossed the 40-item keyset boundary without gaps or duplicates while foreign rows were interleaved; complete status/profile-scoped filter options include rows beyond page one, are cached outside search/filter refreshes, and reject stale cross-profile responses. All 9 repository tests, 3 command tests, and 29 library UI tests passed. |
| Generated Tauri bindings | PASS | Regeneration produced typed `ProblemListPage { items, nextCursor }`, `ProblemListInput.cursor`, and complete `ProblemFilterOptions`; a second generation preserved SHA-256 `28968581EF1E871FDDD5D4B110A70F94A5CB763E82E7049A0A6E9690F4DB52A1`. |
| Rust formatting | PASS | `cargo fmt --check` exited `0` for the remediation tree. |
| Bounded Rust all-target worktree suite | PASS | `scripts/windows-rust-test.ps1` ran `cargo test --all-targets` to exit `0`; library tests reported 159 passed and 3 ignored, and every integration target completed without failure. |
| Full frontend coverage | PASS | Statements 86.55%, branches 81.91%, functions 88.02%, lines 89.41% (`7,213/8,333`, `5,846/7,137`, `1,757/1,996`, `6,485/7,253`). |
| Architecture boundaries | PASS | The 19 frontend dependency-direction assertions and the Rust boundary contract passed. |
| Strict Rust lint | PASS | MSVC all-target Clippy with `-D warnings` exited `0`. |
| Final bounded Rust all-target suite | PENDING | Run `scripts/windows-rust-test.ps1` on the immutable candidate and attach its bounded summary. |
| Immutable-candidate clean-tree contracts | PENDING | Re-run binding and architecture contracts after the intended changes are committed; the binding diff is intentionally uncommitted at this stage. |

## Windows Rust warning classification

| Observation | Status | Evidence |
| --- | --- | --- |
| SQLCipher production memory protection | PASS | Both writable and read-only production connection paths still execute `PRAGMA cipher_memory_security = ON`; no test-only disable switch was added. |
| Basic encrypted open/query/drop | PASS | A focused `sqlcipher_contract` run with phase markers produced no `VirtualLock 1453` warning. |
| Migration transaction | CLASSIFIED | A single non-parallel encrypted database migration produced 26,229 `VirtualLock 1453` warnings between `before-migrations` and `after-migrations`; no warnings appeared after migration, query, or drop. This is not aggregate parallel pressure. |
| OpenSSL debug-symbol warning | CLASSIFIED | A focused link emitted 809 instances of `LNK4099` for missing `ossl_static.pdb`; the test binary linked and passed. |
| All-target warning volume | RECORDED | The final successful worktree run counted 10,661,192 `VirtualLock 1453` lines and 809 OpenSSL PDB lines; one of each was shown and the rest were summarized. |
| Bounded runner contract | PASS | Exit-code preservation, first-occurrence warning display, final counts, timeout code `124`, owned-child termination, and preservation of an unrelated process all passed in `scripts/windows-rust-test.tests.ps1`. |

## Installer and physical-device evidence

| Item | Status | Evidence required |
| --- | --- | --- |
| x64 NSIS installer build and installed GUI smoke | PENDING | Record candidate SHA, workflow run, installer name/size, install/start/single-instance/uninstall result, and result artifact. |
| Native ARM64 installer and smoke | PENDING | Record candidate SHA, workflow run, installer name/size, and native ARM64 smoke result. |
| x64 installer SHA-256 | PENDING | Record the hash from the protected release artifact. |
| ARM64 installer SHA-256 | PENDING | Record the hash from the protected release artifact. |
| Tauri updater signatures and `latest.json` | PENDING | Record both `.sig` files, both `.sha256` files, manifest hash, and exact architecture URLs. |
| Windows 11 x64 at 100%, 125%, 150%, and 200% DPI | PENDING | Verify dashboard, capture, library pagination, review, report, settings, and all modal/drawer boundaries without clipping. |
| Supported Windows 10 x64 | PENDING | Verify install, first launch, capture, encrypted restart, backup/restore, and uninstall as a standard user. |
| Windows 11 ARM64 physical device | PENDING | Verify native process architecture, capture, OCR capability messaging, library pagination, backup/restore, and resource use. |
| 150-image capture workload | PENDING | Record peak working set, CPU, UI responsiveness, completion time, cancellation, and temporary-file cleanup. |
| 1 GB temporary capture workload | PENDING | Record disk growth, peak working set, cleanup after success/cancel/restart, and recovery behavior. |
| Low-disk behavior | PENDING | Verify safe failure during capture, backup, export, and update without corrupting the encrypted library. |
| Unavailable custom storage | PENDING | Verify diagnostics, retry, relocation/recovery, and absence of silent fallback or data loss. |
| Sleep/resume, reboot, and network outage | PENDING | Verify no duplicate process, no lost local mutation, bounded retry, and clear recovery messaging. |

## Unresolved acceptance rows

- Every `PENDING` row above remains blocking for release promotion.
- Existing `docs/windows-*-acceptance.md` checkboxes remain unchanged unless their scenario is actually executed on the candidate build.
- The smart-question-organizing real-corpus evidence is separate uncommitted work and is not claimed by this ledger.
- Authenticode and SmartScreen behavior must be recorded from the actual protected artifacts; an ordinary local unsigned build is not equivalent evidence.

## Promotion decision

**PENDING.** Automated remediation evidence is positive, but the candidate commit, matching CI run, installer hashes/smokes, DPI matrix, 150-image and 1-GB measurements, and low-disk/custom-storage scenarios are not yet captured.
