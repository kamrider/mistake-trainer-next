# v0.1.2 Bug Audit and Coverage Cleanup Implementation Plan

> **For Codex:** Execute this plan in the current `codex/bug-audit-v012` branch. Preserve the user's unrelated untracked hybrid-pipeline plan.

**Goal:** Remove the confirmed automatic-backup durability/UI defects, make frontend coverage describe handwritten code accurately, and verify all frontend, Rust, and Windows release contracts.

**Architecture:** Keep backup policy persistence inside the Rust automatic-backup module, but replace the delete-then-rename sequence with the same write-through atomic replacement semantics already used by startup/storage safety code. Keep UI feedback owned by the existing Settings backup controller. Treat generated TypeScript bindings as a build artifact and exclude only that file from coverage instrumentation.

**Tech Stack:** Rust, Tauri 2, Vue 3, TypeScript, Vitest/V8 coverage, PowerShell release contracts.

---

### Task 1: Preserve the previous automatic-backup policy when replacement fails

**Files:**
- Modify: `src-tauri/src/modules/automatic_backup.rs`

**Steps:**
1. Add a unit test that attempts to replace an existing policy target from a missing source and asserts the target content remains unchanged.
2. Run the focused Rust module test and confirm the regression test fails against delete-then-rename behavior.
3. Add a Windows `MoveFileExW` replacement helper with `MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH`, plus a non-Windows `rename` fallback.
4. Route `write_policy` through the helper and retain same-directory temporary cleanup.
5. Run the focused tests, Rust format, strict Clippy, and the full Rust suite.

### Task 2: Remove stale automatic-backup error feedback after a successful retry

**Files:**
- Modify: `src/app/composables/useSettingsBackupOperations.ts`
- Modify: `src/app/views/SettingsView.vue`
- Modify: `src/app/views/SettingsView.test.ts`

**Steps:**
1. Add a Settings integration test that makes automatic-backup configuration fail once, retry successfully, and asserts the old alert disappears and enabled status is rendered.
2. Run the focused test and confirm it fails.
3. Clear the shared backup message at the beginning of configure/disable operations while preserving truthful errors from the current attempt.
4. Run the focused test plus lint and typecheck.

### Task 3: Make coverage reports reflect maintained source code

**Files:**
- Modify: `vite.config.ts`
- Modify: `src/shared/api/system-status.ts`
- Modify: `src/shared/api/system-status.test.ts`

**Steps:**
1. Remove the unused `systemStatusLabel` duplicate and add error-normalization coverage for the startup helper that production actually calls.
2. Exclude only `src/shared/api/bindings.ts` from V8 coverage because it is generated and separately protected by `bindings:check`.
3. Run `pnpm test:coverage` and record both the updated totals and remaining lowest-covered handwritten modules.
4. Confirm coverage thresholds still pass without weakening any threshold.

### Task 4: Cross-layer verification and review

**Files:**
- Verify: all modified files

**Steps:**
1. Run lint, typecheck, production build, Rust boundaries, bindings freshness, Rust format, strict Clippy, full Rust tests, Windows release contract, and signed-release source contract.
2. Review the final diff against `origin/main`, including user-owned untracked files.
3. Request a focused code review using the available `requesting-code-review` workflow and fix any Critical/Important findings.
4. Commit only the planned files and report remaining risks (notably the already lazy-loaded PDF chunk size warning).
